saved search

noun

A search that a user makes available for later use.

There are many types of saved searches, including reports, alerts, scheduled searches, swimlane searches, and KPIs. All of these saved search types are configured in savedsearches.conf. Saved searches are knowledge objects.
